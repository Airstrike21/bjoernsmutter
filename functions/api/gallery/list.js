export async function onRequestGet({env}){if(!env.BUCKET)return json({error:'R2-Binding BUCKET fehlt.'},500);let cursor;const items=[];do{const r=await env.BUCKET.list({prefix:'approved/',cursor,limit:1000,include:['customMetadata']});items.push(...r.objects.map(o=>({key:o.key,uploadedAt:o.customMetadata?.uploadedAt||o.uploaded})));cursor=r.truncated?r.cursor:undefined}while(cursor);items.sort((a,b)=>new Date(b.uploadedAt)-new Date(a.uploadedAt));return json({items})}function json(data,status=200){return new Response(JSON.stringify(data),{status,headers:{'Content-Type':'application/json; charset=utf-8','Cache-Control':'no-store'}})}
